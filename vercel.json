{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": ".next"
      }
    }
  ],
  "env": {
    "SKIP_ENV_VALIDATION": "1"
  }
}
