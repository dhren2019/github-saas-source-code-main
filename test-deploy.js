const { exec } = require('child_process');

// Test del deploy con un repo pequeño
const testRepo = "dhren2019/githubjobs";
const branch = "main";

console.log(`Testing deploy with repo: ${testRepo}, branch: ${branch}`);

// Aquí podrías hacer una llamada HTTP al endpoint del deploy si lo necesitas
console.log("Deploy test script created. Use this to test deployments.");
